# 知识库系统开发文档

> 最后更新：2026-05-26

---

## 一、项目概述

**目标**：搭建一个轻量、美观、易用的本地知识库系统
**定位**：个人知识管理，替代 Notion/语雀等在线服务
**特点**：本地部署、数据自控、界面美观、响应式支持手机端

**访问地址**：
- 外网：https://wiki.threel.site（Nginx 反代 + SSL）
- 内网：http://192.168.31.58:18800（Uvicorn 直接监听）

---

## 二、技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 后端 | Python FastAPI + Uvicorn | 轻量异步框架 |
| 数据库 | SQLite + FTS5 | 零配置，全文搜索 |
| 前端 | Vue 3 (CDN) | 无构建，单 HTML 文件，Canvas 2D 图谱 |
| 部署 | Nginx 反代 + SSL | 统一端口访问 |
| 服务管理 | systemd | 开机自启，自动重启 |

---

## 三、目录结构

```
/vol1/1000/dev-projects/knowledge-base/
├── start.sh                  # 启动脚本（手动启动用）
├── nginx.conf                # Nginx 配置模板（参考用）
├── DEV.md                    # 快速参考文档
├── 开发文档.md               # 本文档（权威参考）
├── frontend/
│   ├── index.html            # 前端 SPA（唯一入口，~50KB）
│   ├── vue.global.prod.js    # Vue 3.5.34（CDN 本地缓存）
│   └── index.html.bak.*      # 自动备份
├── backend/
│   ├── main.py               # 入口，初始化数据库
│   ├── api.py                # FastAPI 路由
│   ├── models.py             # SQLAlchemy ORM 模型
│   ├── crud.py               # 数据库操作（含 FTS5）
│   └── wiki.db               # SQLite 数据库
├── data/                     # 旧数据目录（已合并到 backend/）
├── extension/                # Chrome 剪藏扩展
└── uploads/                  # 上传文件存储
```

---

## 四、服务架构

### 4.1 整体架构

```
用户浏览器
    │
    ▼
Nginx (80/443) ─── SSL 终止
    │
    ├── /api/*  ─── proxy_pass ───► FastAPI (18800)
    ├── /uploads/* ─── alias ─────► /vol1/1000/dev-projects/knowledge-base/uploads/
    ├── /vue.global.prod.js ───► /vol1/1000/dev-projects/knowledge-base/frontend/
    └── /       ─── try_files ───► /vol1/1000/dev-projects/knowledge-base/frontend/index.html
```

### 4.2 Nginx 配置

配置文件：`/etc/nginx/conf.d/wiki.threel.site.conf`

```nginx
server {
    listen 80;
    server_name wiki.threel.site;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name wiki.threel.site;

    ssl_certificate /usr/trim/var/trim_connect/ssls/wiki.threel.site/1779204508/fullchain.crt;
    ssl_certificate_key /usr/trim/var/trim_connect/ssls/wiki.threel.site/1779204508/wiki.threel.site.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    root /vol1/1000/dev-projects/knowledge-base/frontend;
    index index.html;

    add_header Cache-Control "no-store, no-cache, must-revalidate" always;

    location /api/ {
        proxy_pass http://127.0.0.1:18800;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /uploads/ {
        alias /vol1/1000/dev-projects/knowledge-base/uploads/;
        expires 30d;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

### 4.3 systemd 服务

服务文件：`/etc/systemd/system/knowledge-base.service`

```ini
[Unit]
Description=Knowledge Base Backend
After=network.target

[Service]
Type=simple
WorkingDirectory=/vol1/1000/dev-projects/knowledge-base/backend
ExecStart=/usr/bin/python3 -m uvicorn main:app --host 0.0.0.0 --port 18800
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
```

---

## 五、API 接口规范

### 5.1 笔记接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/notes?skip=0&limit=50&tag=xxx&keyword=xxx` | 列表 |
| GET | `/api/notes/{id}` | 详情 |
| POST | `/api/notes` | 创建 `{title, content, tags[]}` |
| PUT | `/api/notes/{id}` | 更新 `{title?, content?, tags?}` |
| DELETE | `/api/notes/{id}` | 软删除 |
| GET | `/api/notes/{id}/export` | 导出单条 .md |
| GET | `/api/notes/export-all` | 导出全部 ZIP |
| POST | `/api/notes/import` | 批量导入 .md 文件 |

### 5.2 标签接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/tags` | 列表 `[{id, name, color, note_count}]` |

### 5.3 搜索接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/search?q=关键词` | 全文搜索，返回摘要片段 |

### 5.4 图谱接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/graph` | 返回 `{nodes:[{id, title, tags[]}], edges:[{source, target}]}` |

### 5.5 笔记对象结构

```json
{
  "id": 1,
  "title": "笔记标题",
  "content": "Markdown 内容...",
  "summary": "",
  "created_at": "2026-05-21T10:30:00+08:00",
  "updated_at": "2026-05-22T05:45:28+08:00",
  "deleted_at": null,
  "tags": ["标签1", "标签2"]
}
```

---

## 六、数据模型

### 6.1 Note（笔记）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 自增主键 |
| title | String(500) | 标题，默认"无标题" |
| content | Text | Markdown 内容 |
| summary | String(500) | 摘要 |
| created_at | DateTime | 创建时间（UTC+8） |
| updated_at | DateTime | 更新时间（UTC+8） |
| deleted_at | DateTime | 软删除时间，null 表示未删除 |
| tags | [Tag] | 多对多关联 |

### 6.2 Tag（标签）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 自增主键 |
| name | String(100) | 标签名，唯一 |
| color | String(20) | 颜色 |
| created_at | DateTime | 创建时间 |

### 6.3 全文搜索（FTS5）

```sql
CREATE VIRTUAL TABLE note_search USING fts5(
    title, content,
    content='notes',
    content_rowid='id'
);
-- INSERT/UPDATE/DELETE 触发器自动同步
```

---

## 七、前端架构

### 7.1 技术方案

- **单 HTML 文件**：所有 CSS/JS 内联，无构建步骤
- **Vue 3.5.34**：CDN 引入，Composition API
- **响应式**：纯 CSS 媒体查询，支持手机端

### 7.2 核心功能

1. **笔记列表**：侧边栏笔记列表，按更新时间倒序
2. **阅读/编辑模式**：默认阅读模式，支持 Markdown 渲染
3. **搜索**：防抖 300ms，全文搜索
4. **标签筛选**：点击标签过滤笔记
5. **知识图谱**：Canvas 2D 力导向布局，节点大小按度数缩放
6. **主题切换**：深色/浅色，跟随系统或手动切换
7. **[[wikilink]]**：交叉引用，点击跳转

### 7.3 知识图谱

- **渲染**：Canvas 2D，requestAnimationFrame 动画循环
- **布局**：力导向（斥力 + 引力 + 中心重力）
- **交互**：拖拽节点、滚轮缩放、双击打开笔记、悬停高亮
- **节点大小**：根据连接度数动态缩放（3-16px）
- **配色**：按标签分组，莫兰迪色系
- **背景**：跟随页面主题（深色/浅色）

### 7.4 快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl+S | 保存（编辑模式） |
| Ctrl+N | 新建笔记 |
| Ctrl+E | 切换编辑/阅读模式 |

---

## 八、部署与运维

### 8.1 常用命令

```bash
# 服务管理
sudo systemctl status knowledge-base
sudo systemctl restart knowledge-base
sudo systemctl status nginx
sudo nginx -t && sudo nginx -s reload

# 日志
sudo journalctl -u knowledge-base -f
sudo tail -f /var/log/nginx/error.log

# 前端修改 → 直接生效（静态文件）
vim /vol1/1000/dev-projects/knowledge-base/frontend/index.html

# 后端修改 → 重启服务
vim /vol1/1000/dev-projects/knowledge-base/backend/api.py
sudo systemctl restart knowledge-base
```

### 8.2 数据备份

```bash
cp /vol1/1000/dev-projects/knowledge-base/backend/wiki.db \
   /vol1/1000/dev-projects/knowledge-base/backend/wiki.db.$(date +%Y%m%d)
```

---

## 九、修改记录

### 2026-05-27

1. **标签颜色优化**：
   - 将数据库中的标签颜色更新为莫兰迪色系（15种柔和色调）
   - 前端标签按钮使用API返回的颜色
   - 知识图谱节点颜色与标签颜色同步
2. **标签管理功能**：
   - 新增标签编辑API（PUT /api/tags/{id}）
   - 新增标签删除API（DELETE /api/tags/{id}）
   - 前端添加标签管理模态框
   - 侧边栏添加标签管理按钮（⚙）
3. **笔记列表分页**：
   - 实现分页加载，默认每页50条
   - 添加"加载更多"按钮
   - 显示已加载/总数信息
4. **搜索历史功能**：
   - 自动保存最近10条搜索记录
   - 搜索框聚焦时显示历史下拉
   - 支持一键清空搜索历史
5. **笔记模板功能**：
   - 新增6种常用模板：工作日记、会议记录、技术文档、学习笔记、读书笔记、项目计划
   - 侧边栏添加模板入口
   - 点击模板快速创建新笔记

### 2026-05-26

1. **去掉所有绿色**：将所有 `var(--green)` 替换为 `var(--text)`，TAG_COLORS 改为莫兰迪色系
2. **知识图谱优化**：
   - 节点大小按度数动态缩放（3-16px）
   - 高 degree 节点显示外圈和光晕
   - 悬停显示标签，大节点始终显示标签
   - 背景跟随页面主题（深色/浅色）
   - 底部右侧标签图例，左上角统计信息
3. **路径修正**：前端文件路径从 `/vol1/1000/knowledge-base/` 改为 `/vol1/1000/dev-projects/knowledge-base/`

### 2026-05-23

1. 新增知识图谱可视化（Canvas 2D 力导向布局）
2. 新增 [[wikilink]] 交叉引用
3. 新增笔记模板（6 种）
4. 新增搜索关键词高亮
5. 新增图片粘贴上传
6. 新增回收站（软删除）
7. 新增批量导入/导出
8. 新增 Chrome 剪藏扩展
9. 新增目录/大纲导航（TOC）
10. 暗色/亮色主题切换

### 2026-05-22

1. 修复 Markdown 换行问题
2. 双栏改单栏（阅读/编辑模式切换）

---

## 十、后续计划

- [x] 标签管理（编辑/删除）
- [x] 笔记模板
- [x] 搜索历史
- [x] 分页加载
- [ ] 多用户支持
- [ ] 协作编辑
- [ ] AI 辅助写作
- [ ] 插件系统
- [ ] 标签颜色选择器
- [ ] 笔记版本历史
- [ ] 批量操作（批量删除、批量标签）
- [ ] 导出为PDF
- [ ] 快捷键自定义
